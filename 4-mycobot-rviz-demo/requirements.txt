# Python deps for the myCobot 280 sine-path demo (install into ./venv)
#   python3 -m venv --system-site-packages venv
#   . venv/bin/activate && pip install -r requirements.txt
ikpy>=3.4
scipy>=1.10
numpy>=1.24
matplotlib>=3.6
